class K3d:
    CREATE = 'create'
    DELETE = 'delete'

    CLUSTER = 'cluster'

    VERSION = '--image'
    NODES = '--servers'

    ARG = '--k3s-arg'

    DISABLE_TRAEFIK = '--disable=traefik@server:*'

    LOADBALANCER = '@loadbalancer'
    PORT = '--port'
